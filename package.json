{
  "name": "simple-type-schema",
  "version": "1.0.0",
  "description": "Define a schema with a class",
  "main": "index.ts",
  "scripts": {
    "start": "tsmon src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "test": "npm run build && mocha \"./test/*.ts\" --timeout 15000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "simple-schema": "^1.1.0"
  },
  "devDependencies": {
    "mocha": "^6.2.0",
    "reflect-metadata": "^0.1.13"
  }
}
